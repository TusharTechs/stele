node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
.env*
!.env.example
.data/
next-env.d.ts
tsconfig.tsbuildinfo

# public/seed/ and demo/ are the shipped demo bundle (see scripts/seed.mts) and are tracked
# deliberately, so a clean clone shows finished productions without a key or any spend.
.vercel

# Editor and agent tooling. Regenerated locally, of no use to anyone reading this repo, and
# `AGENTS.md` in particular is rewritten by `next dev` on every start.
.claude/
AGENTS.md
CLAUDE.md
